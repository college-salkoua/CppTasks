#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	int x;
	int y;
	int z;
	int aanswer;
	int banswer;
	cout << "Введіть x\n";
	cin >> x;
	cout << "Введіть y\n";
	cin >> y;
	cout << "Ведіть z\n";
	cin >> z;
	aanswer = ((1 + pow(sin(x+y), 2)) / 2 + mod(x - 2 * x)) / (1 + pow(x, 2) * pow(y, 2));
	banswer = pow(cos(atan(1/z), 2);
	cout << aanswer;
	cout << banswer;
	cin.get();
	return 0;
}
